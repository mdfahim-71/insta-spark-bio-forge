import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mdfahim.instaspark',
  appName: 'InstaSpark Bio Forge',
  webDir: 'dist'
};

export default config;
